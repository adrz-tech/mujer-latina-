# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# Supabase PostgreSQL Relational Database Configuration
# Obtain both values in your Supabase Dashboard: Project Settings -> API
# Use ONLY the Project URL and the Publishable/anon key (never use service_role key in frontend!)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
